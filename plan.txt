- finnas med en rabattkod: FREEBIE
- kunna hantera 2 klick samtidigt för t.ex den sista vip biljetten
- skriv in varje köp i en loggfil

filer: 
buy ticket.php
    tar emot: user_id (int)
              total_amount (int)
              product:id (float/double)

api handler.php
    check stock count based on id
    add order
    check user balance 

kanske: 
    göra det möjligt att ändra balance i databasen
    göra det möjligt att ändra stock_count i databasen
    göra det möjligt att ändra pris i datab